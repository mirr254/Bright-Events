language: python
python:
 - "3.6.1"

#dependacies and libraries to install
install: pip install -r requirements.txt

script:
- nosetests tests/test_app_tests.py --with-coverage

after_success:
 - coveralls --config_file .coveragerc
 - coveralls
  